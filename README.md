# running our project:
pip install -r requirements.txt
python main.py

recommended to put in your SpoonOS API keys to actually run the agents' tasks.

API root: http://localhost:8000

# architecture/project schema:

